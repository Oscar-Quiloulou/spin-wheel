// leaderboard.js
// Gestion du leaderboard (lecture + écriture dans /scores).

function initLeaderboardListener() {
  // TODO: écouter /scores dans Firebase et remplir #leaderboardList
}

function pushScoreToLeaderboard(score) {
  // TODO: pousser {pseudo, score, date} dans /scores
  // On décidera plus tard comment tu veux gérer le pseudo (prompt, localStorage, etc.)
}
